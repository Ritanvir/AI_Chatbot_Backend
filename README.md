# AI Chatbot (FastAPI + OpenAI)

A simple chatbot backend built with **FastAPI** and **OpenAI**.  
Frontend can call this backend to chat in real time.

---

## Features
- FastAPI REST API (`POST /chat`)
- Uses OpenAI Chat Completions
- Supports conversation history
- Safe API-key handling with `.env`
